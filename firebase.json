{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "source": "packages/api",
    "predeploy": ["if [ \"$(uname)\" == \"Darwin\" ]; then yarn api:build; fi"]
  }
}
